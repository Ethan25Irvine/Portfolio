import React from "react";
import resume from "./resume.png"

const Resume = () => {
    return (
        <div className="container">
          <img src={resume} width="100%"></img> 
        </div>
        
    )
}

export default Resume;